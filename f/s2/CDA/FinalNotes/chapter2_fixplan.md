# Chapter 2: Local Fix Plan

## Objective
Fix localized layout issues in `main/chapters/chapter2.tex`.

## Actionable Steps
1. **List Padding:** 
   - Line 25: Add blank line before `\begin{itemize}` (Binary to Decimal example).
   - Line 35: Add blank line before `\begin{enumerate}` (Decimal to Binary steps).
   - Line 46: Add blank line before `\begin{itemize}` (Decimal to Binary result).
   - Line 95: Add blank line before `\begin{enumerate}` (2's complement steps).
   - Line 112: Add blank line before `\begin{itemize}` (Sign Extension).
   - Line 141: Add blank line before `\begin{itemize}` (Signed Addition conditions).
   - Line 151: Add blank line before `\begin{itemize}` (XOR Rule logic).

2. **Math Spacing:**
   - Ensure display math `\[ ... \]` on Lines 14 and 24 are surrounded by blank lines to prevent cramping against text.

3. **Environment Proximity:**
   - Line 60: Add `\vspace{10pt}` before `\begin{vocabulary}[Sign Bit]` to separate it from the Section 3 header.